/** Powyżej tej liczby graczy ukrywamy pełną macierz typów (zbyt ciężka w przeglądarce). */
export const TIPS_MATRIX_USER_LIMIT = 30;

/** Ile miejsc pokazujemy w tabeli rankingu (+ pozycja zalogowanego, jeśli poza top N). */
export const RANKING_TOP_N = 10;
